# MCP Server Bridge Restish

## Claude Desktop

claude_desktop_config.json

```json
{
  "mcpServers": {
    "bridge_restish": {
      "command": "npx",
      "args": [
        "mcp-server-bridge-restish",
        "http://localhost:1880",
        "XXXXXXXXXXX"
      ]
    }
  }
}
```
